services:
  postgres:
    image: postgres:16
    container_name: pct-postgres
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pct_postgres_data:/var/lib/postgresql/data

volumes:
  pct_postgres_data:
